/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { GamesPipe } from './games.pipe';

describe('GamesPipe', () => {
  it('create an instance', () => {
    let pipe = new GamesPipe();
    expect(pipe).toBeTruthy();
  });
});
